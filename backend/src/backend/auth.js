// Function checks if user exists in database and logs them in
// can return either error or an identifier?
export function login(email, password) {
  // TODO!
  return null
}

// Function registers user to database
// can return either error or an identifier?
export function register(email, password, username, firstName, lastName) {
  // TODO!
  return null
}