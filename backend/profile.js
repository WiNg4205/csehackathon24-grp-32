// Function returns the relevant information for a user
// can return profile pic, most recent habbits, current streaks
export function getProfile(user) {
  // TODO!
  return null
}

// Function returns the list of friends correlating to the user
// can simply return the friends list from the DB
export function getFriendsList(user) {
  // TODO!
  return null
}

// Function retrieves an habits in common between user1 and user2
// return the first three shared habits (if any)
export function getCommonHabits(user1, user2) {
  // TODO!
  return null
}

// Function sends a friend request from first user to the second user
// return null?
export function addFriend(userSendingFriend, userReceivingFriend) {
  // TODO!
  return null
}

// Function removes the users from each other's friends list
// return null?
export function removeFriend(user1, user2) {
  // TODO!
  return null
}